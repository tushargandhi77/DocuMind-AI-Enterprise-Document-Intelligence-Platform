version: "3.9"

services:
  api:
    build: .
    container_name: enterprise_rag_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app   # Enables hot reload in dev
    restart: unless-stopped
